[project]
name = "paradox-agent"
version = "0.1.0"
description = "The Paradox agent: a Windows computer assistant that decides how to do what you ask."
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.40",
    "websockets>=13",
    "python-dotenv>=1.0",
    "mss>=9.0",
    "Pillow>=10.0",
    "psutil>=6.0",
    "google-cloud-speech>=2.24",
    "google-cloud-texttospeech>=2.16",
    "pywin32>=306; sys_platform == 'win32'",
    "uiautomation>=2.0; sys_platform == 'win32'",
    "winrt-Windows.Media.Control>=3.2; sys_platform == 'win32'",
    "winrt-Windows.Media>=3.2; sys_platform == 'win32'",
    "winrt-Windows.Storage.Streams>=3.2; sys_platform == 'win32'",
    "winrt-Windows.Foundation>=3.2; sys_platform == 'win32'",
]

[project.optional-dependencies]
# The agent runs on any one of these. Install the extra for the key you have;
# `pip install -e ".[all]"` pulls all three.
openai = ["openai>=1.60"]
google = ["google-genai>=0.8"]
# Neural TTS voices (PARADOX_TTS=edge) — many more languages/accents than the
# local SAPI voices, at the cost of sending reply text to Microsoft's endpoint.
edge = ["edge-tts>=6.1"]
all = ["openai>=1.60", "google-genai>=0.8", "edge-tts>=6.1"]

[project.scripts]
paradox = "paradox.__main__:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["paradox*"]
